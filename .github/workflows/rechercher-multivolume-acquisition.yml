name: Rechercher — governed multi-volume acquisition

on:
  push:
    branches:
      - 'rechercher/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  acquire:
    uses: lalibimohamed-maker/-dorar-hadith-api-official/.github/workflows/rechercher-governed-acquisition.yml@main
    permissions:
      contents: write
