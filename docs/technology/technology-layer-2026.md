# طبقة التقنيات — Technology Layer 2026

هذه الطبقة تسجل التقنيات المفتوحة والمعايير العامة التي تعتمد عليها موسوعة دين الله، مع إبقاء الاختيار قابلاً للقياس والترقية.

## مبدأ التكلفة

- تُفضّل المعايير المفتوحة والبرمجيات الحرة/مفتوحة المصدر والاستضافة الذاتية.
- «مجاني» يعني أن البرنامج/المعيار متاح دون رسوم ترخيص وفق شروطه؛ ولا يعني أن الخوادم أو التخزين أو GPU مجانية إلى الأبد.
- لا يُعتمد على خدمة مدفوعة أو مغلقة عندما يوجد بديل مفتوح مناسب دون توثيق سبب الاستثناء.

## Web Platform

- PWA / Service Workers / Cache APIs.
- IndexedDB.
- Web Workers / Shared Workers.
- WebRTC وWeb Share.
- Web Crypto / CSP / SRI.
- Web Components.

## الأداء والحوسبة

- WebAssembly (Wasm).
- SIMD/threads عند دعم المتصفح والعتاد وبعد القياس.
- WebGPU مع fallback آمن.
- WebCodecs.
- Web Audio API.

## الواجهة والوصول

- HTML/CSS/TypeScript.
- RTL وUnicode-first للعربية.
- WCAG وARIA وإتاحة لوحة المفاتيح وقارئات الشاشة.
- Responsive design وprogressive enhancement.

## البناء والتطبيق

- Vite/Rolldown.
- TypeScript للتحقق الساكن.
- Lockfiles وreproducible builds حيثما أمكن.
- Web Workers/Wasm لتقليل ضغط main thread.

## البيانات والتكامل

- JSON/JSONL.
- JSON-LD/Schema.org.
- OpenAPI.
- HTTP caching وETag وcontent hashes.

## البحث والذكاء الدلالي

- Meilisearch للبحث النصي.
- Qdrant للبحث الدلالي والهجين.
- Reranking/embeddings كطبقة استرجاع فقط.
- المصدر الأصلي وprovenance يظلان المرجع النهائي.

## لوحة المفاتيح والإدخال

- **Keyman**: المسار الرئيسي متعدد المنصات؛ يدعم العربية وتخطيطات متعددة، ويُقيّم بحسب الجهاز والإصدار.
- **OpenBoard**: مسار Android/FOSS.
- **AnySoftKeyboard**: مسار Android للاقتراح والتصحيح، مع حزمة عربية عند توفرها.
- **ArabicIME**: مسار تجريبي لإدخال العربية على سطح المكتب.
- **Arabic Intelligent Input Layer**: طبقة موسوعة دين الله للتطبيع والبحث العربي، مع إبقاء النص الأصلي بلا تعديل.
- تطبيع Unicode والتشكيل والرموز القرآنية يتم في الإدخال/البحث فقط، ولا يغير Corpus أو النصوص الأصلية.

## الوسائط والصوت

- AVIF/WebP مع fallback مناسب.
- Sharp لمعالجة الصور.
- Real-ESRGAN وSwinIR للترميم/رفع الدقة بعد اختبار الجودة.
- FFmpeg للصوت والفيديو.
- DeepFilterNet وRNNoise لتنقية الصوت.

### «يا بوابة العلم» — Voice Layer

الهدف: تجربة صوتية عربية هادئة ودقيقة تشبه المساعدات الصوتية، مع معالجة محلية أولًا حيثما أمكن.

- **Wake word:** `openWakeWord` كإطار مفتوح، مع نموذج عربي مخصص مقترح باسم `ya_bawabat_al_ilm`. النماذج الجاهزة الحالية في openWakeWord تركز على الإنجليزية؛ لذلك لا يُعلن دعم العربية الجاهز قبل تدريب النموذج واختباره. `micro-wake-word` مسار بديل للأجهزة المحدودة.
- **VAD:** `Silero VAD` لعزل الكلام وتقليل التفعيلات الكاذبة.
- **STT الرئيسي:** `whisper.cpp` للتعرف المحلي متعدد اللغات، مع `faster-whisper` كبديل عالي الأداء.
- **تحسين الصوت:** `DeepFilterNet` أو `RNNoise` بحسب latency/quality؛ لا نطبقهما معًا افتراضيًا إلا بعد benchmark.
- **TTS الرئيسي:** `Piper1-GPL` محليًا، مع صوت عربي `ar_JO` متاح، والتحقق من ترخيص كل نموذج صوت قبل توزيعه.
- **TTS المتقدم/الاحتياطي:** `sherpa-onnx` للـruntime المحلي عندما يثبت تفوقه؛ تُراجع تراخيص النماذج الصوتية منفصلًا عن ترخيص المحرك.
- **Voice-cloning research:** `OpenVoice V2` يُحفظ ضمن مسار تجريبي؛ كوده V1/V2 MIT، لكن استخدام أي صوت مرجعي أو نموذج صوت لشخص حقيقي يحتاج إذنًا/ترخيصًا صالحًا من صاحب الصوت قبل النشر أو التوزيع. citeturn305754search2
- **Web fallback:** Web Speech API عند ملاءمة المتصفح، مع تفضيل on-device recognition عندما تكون متاحة؛ ليست backend موثوقة وحيدة لأن بعض المتصفحات قد تستخدم خدمة خادمية.

### «جوهري وهادئ» — مواصفات الجودة

- loudness normalization ثابت ومدروس.
- إزالة ضوضاء دون تشويه الصوامت والحركات العربية.
- الحفاظ على مخارج الحروف والوقفات في النصوص العربية.
- عدم استعمال pitch-shifting أو voice conversion على التلاوة الأصلية.
- اختبار intelligibility وWER/CER وfalse-accept/false-reject قبل اعتماد إعداد افتراضي جديد.
- مسار منخفض الكمون للمحادثة، ومسار أعلى جودة للقراءة الطويلة.

### فصل صوت المساعد عن التلاوة القرآنية

**هذا فصل إلزامي معماريًا:**

1. صوت «يا بوابة العلم» هو صوت مساعد اصطناعي عام وغير منسوب إلى قارئ أو شخص حقيقي.
2. القرآن لا يُنسب إلى صوت صناعي لشخص حقيقي لمجرد تقليده.
3. عند طلب «اقرأ الآية»، يفضّل النظام تشغيل تسجيل تلاوة أصلي موثوق مرتبط بالسورة والآية والرواية والقارئ، مع حفظ provenance وحقوق الاستخدام.
4. إذا كان التسجيل الرسمي/المرخّص غير متاح، لا تُسمّى التلاوة بصوت قارئ معين ولا يُنشأ clone له؛ يمكن استخدام نص مقروء بصوت مساعد عام فقط إذا كان ذلك مناسبًا ومصرحًا به.
5. لا يُدرّب نموذج صوت على تسجيلات قارئ حقيقي ولا يُوزع voice profile له إلا بعد إثبات الإذن/الترخيص المناسب.
6. ملفات التلاوة والتسجيلات المرجعية تبقى منفصلة عن بيانات النصوص العلمية والـCorpus.

مجمع الملك فهد لطباعة المصحف الشريف يتيح على موقعه تلاوات مرتلة لقراء معتمدين، وينص في صفحة الصوتيات على إتاحة النسخ الرقمية المدرجة للاستخدام العام مجانًا في التطبيقات والمواقع والبث، وفق الإطار المعلن هناك. ويجب الرجوع إلى شروط المصدر المحدد والقارئ المحدد عند اختيار تسجيل بعينه. citeturn954313search0turn954313search10

## إعدادات الهاتف والويب

- الميكروفون: تشغيل/إيقاف.
- wake word: تشغيل/إيقاف.
- حساسية «يا بوابة العلم» قابلة للضبط.
- مصدر الإدخال: ميكروفون الجهاز أو سماعة/بلوتوث عندما يسمح النظام.
- اللغة واللهجة والـlocale.
- سرعة الكلام وpitch والوقفات ومستوى الصوت.
- اختيار الصوت لكل لغة.
- وضع الخصوصية: محلي فقط / خدمة ويب عند السماح الصريح.
- حفظ التسجيلات: متوقف افتراضيًا.
- مؤشر الاستماع وإيقاف فوري.
- اختصار زر/لوحة مفاتيح عند عدم دعم wake word.
- اختيار مسار «المساعد» أو «التلاوة» بصورة صريحة.
- قفل تغيير قارئ التلاوة خلف فحص الحقوق/المصدر.

## الصوت والخصوصية والأمان

- موافقة صريحة على الميكروفون.
- لا تسجيل في الخلفية افتراضيًا.
- معالجة محلية أولًا.
- لا استخدام لخدمة Web Speech الخارجية إلا مع كشف المسار وموافقة صريحة عندما تكون المعالجة خادمية.
- `getUserMedia()` يتطلب secure context، والوصول إلى الميكروفون يخضع لأذونات المتصفح وPermissions Policy.
- `Permissions-Policy: microphone=(self)` كقاعدة افتراضية للموسوعة.
- الصوت الخام لا يدخل الـCorpus ولا يصبح مصدرًا علميًا.

## التنزيل والتخزين

- aria2 / Wget2 / curl للتنزيلات العامة المصرح بها.
- rclone لمسارات التخزين المتعددة.
- yt-dlp للوسائط المسموح بتنزيلها فقط.
- qBittorrent للتوزيع P2P للمحتوى المصرح به فقط.

## المراقبة والاعتمادية

- OpenTelemetry للـtraces/metrics/logs.
- Grafana OSS للتصور والتنبيه.
- Kopia وRestic للنسخ الاحتياطي والاستعادة.

## الأمن وسلسلة التوريد

- CodeQL.
- Trivy / Grype / OSV-Scanner.
- Syft للـSBOM.
- Gitleaks.
- Zizmor.
- Scorecard.
- تثبيت Actions على commit SHA حيث تسمح البنية.

## قواعد الاختيار

كل تقنية تُقيّم على:

1. الأمن.
2. الترخيص.
3. الكلفة التشغيلية.
4. الأداء.
5. دعم العربية وRTL عند الصلة.
6. النضج والتوافق.
7. نشاط الصيانة.
8. قابلية الاستضافة الذاتية.
9. قابلية إعادة الإنتاج.
10. الإسناد والحقوق.

التصنيف: `Adopt` / `Trial` / `Assess` / `Hold` / `Deprecated`.

لا تتم ترقية تقنية تلقائيًا لمجرد صدور إصدار أحدث؛ الترقية الكبيرة تحتاج benchmark ومراجعة وتحققًا من الأمن والتوافق وعدم فقدان provenance.

## سياسة «مجاني»

لا نعد المستخدم بأن أي خدمة استضافة أو بنية تحتية ستبقى مجانية مدى الحياة. نثبت فقط البرمجيات والمعايير التي تسمح تراخيصها بالاستخدام المطلوب، ونفضّل self-hosting لتقليل الاعتماد على الخدمات المدفوعة.
