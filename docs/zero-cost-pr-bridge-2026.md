# Zero-Cost PR Bridge

This change set is prepared on a review branch so the protected encyclopedia branch is never modified outside the normal pull-request process.

The bridge carries the verified universal-free distribution policy, five-language localization contract, PWA baseline, offline/local search architecture, large-asset distribution controls, and GitHub/Internet Archive trust-boundary rules.
