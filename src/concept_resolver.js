import { conceptCard } from './corpus_api_contract.js';
export function resolveSelectedConcept(selectedText,contextId,language='ar',records=[]) { const text=String(selectedText||'').trim(); const context=records.find(r=>r.id===contextId); const candidates=records.filter(r=>[r.title_ar,r.title,r.id].filter(Boolean).map(String).some(v=>v===text||v.includes(text))); const record=context&&candidates.length===0?context:candidates[0]||context; return conceptCard({term:text,contextId,language,record}); }
export function buildKnowledgeSections(record,knowledge={}) { const sections=['definition','contextual_meaning','quran_evidence','hadith_evidence','tafsir','aqidah','fiqh','seerah','scholarly_views','related_concepts','translations','sources']; return sections.reduce((out,s)=>{if(knowledge[s]!==undefined)out[s]=knowledge[s];return out;},{id:record?.id||null}); }
