import { searchResponse, conceptCard, bilingualResult } from './corpus_api_contract.js';
export function searchCorpus(query, options = {}, records = []) { const language=options.language||'ar'; const bilingual=Boolean(options.bilingual); const normalized=String(query||'').trim().toLowerCase(); const matches=records.filter(r=>[r.title_ar,r.title,r.id].filter(Boolean).some(v=>String(v).toLowerCase().includes(normalized))); return searchResponse({query,language,bilingual,results:matches}); }
export function resolveConcept(term,contextId,language='ar',records=[]) { const record=records.find(r=>r.id===contextId)||records.find(r=>r.title_ar===term||r.title===term); return conceptCard({term,contextId,language,record}); }
export function makeBilingual(originalArabic,translation,targetLanguage) { return bilingualResult(originalArabic,translation,targetLanguage); }
